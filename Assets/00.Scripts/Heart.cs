using DG.Tweening;
using UnityEngine;

public class Heart : MonoBehaviour
{
    private RectTransform rectTransform;
    
    
    
    // private void HeartShake()
    // {
    //     rectTransform.DOShakePosition(duration, shakeStrength, 10, 90, false, true)
    //         .OnComplete(() => uiElement.anchoredPosition = Vector2.zero);
    // }
}
